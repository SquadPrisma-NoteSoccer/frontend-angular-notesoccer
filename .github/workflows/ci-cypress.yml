name: Cypress Tests (Chrome + Cloud)

on:
  push:
        branches: [test]
        paths:
            - 'cypress/**'
            - 'package.json'
            - 'package-lock.json'
  pull_request:
        branches:
            - staging
        paths:
            - 'cypress/**'
            - 'package.json'
            - 'package-lock.json'
  workflow_dispatch: # permite rodar manualmente pelo GitHub
        inputs:
            browsers:
                description: "Escolher navegadores (chrome, edge, firefox, electron)"
                required: false
                default: "chrome"

jobs:
    test:
        timeout-minutes: 120
        runs-on: ubuntu-latest
        strategy:
          matrix:
            browser: [chrome]        
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: lts/*
            - name: Install dependencies
              run: npm ci

            - name: Verify Cypress installation
              run: npx cypress verify

            - name: Run Cypress tests
              uses: cypress-io/github-action@v5
              with:
                  browser: ${{ matrix.browser }}              
                  record: true                 # envia resultados para o Cypress Dashboard
                  parallel: true              # ajuste se quiser rodar paralelo
                  group: "GitHub Actions - ${{ matrix.browser }}"  # nome do grupo no Dashboard
              env:
                  CYPRESS_PROJECT_ID: ${{ secrets.CYPRESS_PROJECT_ID }}
                  CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}

        #   #  Upload de screenshots e vídeos (se falhar)
        #   - name: Upload Cypress artifacts (${{ matrix.browser }})
        #     if: failure()
        #     uses: actions/upload-artifact@v4
        #     with:
        #       name: cypress-artifacts-${{ matrix.browser }}
        #       path: |
        #         cypress/screenshots
        #         cypress/videos
